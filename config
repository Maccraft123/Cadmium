#!/bin/false
# TODO: fancy menu?

# Target device
# It is sorta using codenames, but "kukui" sounds stupid so i won't use it
# One of: bob, duet, kevin, minnie, speedy
if [ "$RELEASE" = "" ]; then
	TARGET=minnie
else
	TARGET=$RELEASE
fi

# RootFS type
# One of: debian, void, void-musl
ROOTFS=debian
