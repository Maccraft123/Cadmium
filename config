#!/bin/false
# TODO: fancy menu?

# Target device
# It is sorta using codenames, but "kukui" sounds stupid so i won't use it
# One of: bob, duet, kevin, minnie, speedy, lazor
if [ "$RELEASE" = "" ]; then
	TARGET=duet
else
	TARGET=$RELEASE
fi

# RootFS type
# One of: debian, void, void-musl
ROOTFS=debian

# Build Cadmium with Oxide support(dualboot)
# 0: off 1: on
B_OXIDE=0

# Which kernel should be built. disables firmware
# kernelorg: from kernel.org libre: from linux-libre.fsfla.org cros: chrome os kernel
KERNEL=cros
