#!/bin/false
# TODO: fancy menu?

# Target device
# It is sorta using codenames, but "kukui" sounds stupid so i won't use it
# TODO: maybe this should be baseboard? veyron, gru, kukui or whatever?
# One of: duet, kevin, minnie, speedy
TARGET=duet

# Kernel variant
# One of: stable, testing
KERNEL=stable

# RootFS type
# One of: debian, void, void-musl
ROOTFS=debian
