#!/bin/false
# TODO: fancy menu?

# Target device
# It is sorta using codenames, but "kukui" sounds stupid so i won't use it
# One of: duet, kevin, minnie
TARGET=duet

# Kernel variant
# One of: stable, testing
KERNEL=stable
#TODO


# RootFS type
# One of: debian, void, void-musl
ROOTFS=debian
